syntax = "proto3";

package verifyMfa;

service VerifyMfaProxyService {
  rpc getUserId (VerifyMfaRequest) returns (VerifyMfaResponse);
}

message VerifyMfaRequest {
  string email = 1;
}

message VerifyMfaResponse {
  string userId = 1;
}